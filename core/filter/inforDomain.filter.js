export default function (inforDomain) {
  if (inforDomain === 3) {
    return '私募';
  } else if (inforDomain === 5) {
    return '银行';
  } else if (inforDomain === 4) {
    return '保险';
  } else if (inforDomain === 1) {
    return '信托';
  } else if (inforDomain === 6) {
    return '证券';
  }
}
